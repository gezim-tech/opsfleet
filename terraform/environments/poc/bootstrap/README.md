# POC Backend Bootstrap

Creates remote-state infrastructure for this environment:

- S3 bucket: `opsfleet-tfstate-624654857501`
- DynamoDB lock table: `terraform-state-locks`

Run:

```bash
terraform init
terraform apply
```
